<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.motion.widget.MotionLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activities.MainActivity"
        app:motionDebug="SHOW_ALL"
        app:layoutDescription="@xml/activity_main_scene">

    <androidx.fragment.app.FragmentContainerView
            android:id="@+id/contentFrame"
            android:name="androidx.navigation.fragment.NavHostFragment"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/nav_view" />


    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/nav_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="?android:attr/windowBackground"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:menu="@menu/bottom_nav_menu" />

    <View
            android:id="@+id/nowPlayingContainer"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:elevation="20dp"
            android:background="@android:color/white"
            app:layout_constraintBottom_toTopOf="@+id/nav_view"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:layout_height="55dp">

        <ImageView
                android:id="@+id/imageCoverPlaying"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerCrop"
                android:src="@drawable/tsumugi_ssr"
                app:layout_constraintBottom_toBottomOf="@id/nowPlayingContainer"
                app:layout_constraintStart_toStartOf="@id/nowPlayingContainer"
                app:layout_constraintTop_toTopOf="@id/nowPlayingContainer" />

        <TextView
                android:id="@+id/nowPlayingTitleText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tsumugi Shiraishi"
                app:layout_constraintEnd_toEndOf="@id/nowPlayingContainer"
                app:layout_constraintTop_toTopOf="@id/nowPlayingContainer"
                app:layout_constraintBottom_toBottomOf="@id/nowPlayingContainer"/>

    </View>


</androidx.constraintlayout.motion.widget.MotionLayout>